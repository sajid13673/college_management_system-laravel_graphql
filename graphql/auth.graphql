extend type Mutation{
    login(email: String!, password: String!, device_name: String!): Auth
    logout: String! @guard
}
type Auth{
    token: String
    role: String
}
extend type Query{
    me: User! @auth @guard
}